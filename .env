# This file is committed to git, so it holds no real secrets. The AI
# provider key and Flask secret key actually used to run the app locally
# live in .env.example instead, which is gitignored. See the README.

PROVIDER=anthropic
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MODEL=claude-haiku-4-5-20251001
FLASK_SECRET_KEY=

# Shared password for the staff dashboard at /dashboard. Kept here, not
# blanked out, since anyone reviewing this needs it to actually log in.
DASHBOARD_PASSWORD=kettle-c5d454e1
